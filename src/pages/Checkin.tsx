/**
 * Checkin (page entry-point) — re-export do container decomposto.
 * Implementação real em `src/features/checkin/` (ADR-0050, Story 3.2 reconciliada).
 */
export { Checkin, default } from '@/features/checkin/Checkin.container'
