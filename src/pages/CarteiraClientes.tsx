/**
 * Carteira de Clientes (page entry-point) — re-export do container do CRM do vendedor.
 * Implementação em `src/features/crm/CarteiraClientes.container.tsx`.
 */
export { CarteiraClientes, default } from '@/features/crm/CarteiraClientes.container'
