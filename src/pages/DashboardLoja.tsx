/**
 * @deprecated A implementação foi movida para `src/features/dashboard-loja/`.
 * Este arquivo permanece como re-export para preservar imports legados
 * e a rota `/lojas/:storeSlug`. Story 2.5 (UX-001) — ADR-0050.
 */
export { DashboardLoja, default } from '@/features/dashboard-loja/DashboardLoja.container'
